[workspace]
members = [
    "stm32-core",
    "pico-network",
    "shared-protocol"
]
resolver = "2"

[profile.release]
codegen-units = 1 # Optimize for size and speed
lto = true        # Link-time optimization (crucial for embedded)
opt-level = "z"   # Optimize for binary size

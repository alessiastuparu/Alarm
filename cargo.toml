[workspace]
members = [
    "stm32-core",
    "pico-network",
    "shared-protocol"
]
resolver = "2"

[profile.release]
panic = "abort"
codegen-units = 1 
lto = true        
opt-level = "z"   

[profile.dev]
panic = "abort"

